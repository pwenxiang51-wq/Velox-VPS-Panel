# 🛰️ VeloX VPS Panel v6.2 (全域兼容满血终极版)

[![Linux](https://img.shields.io/badge/OS-Ubuntu%20%7C%20Debian%20%7C%20CentOS-orange.svg)]()
[![Bash](https://img.shields.io/badge/Language-Pure%20Bash-blue.svg)]()
[![Armor](https://img.shields.io/badge/Armor-Idiot--Proof%20%7C%20Anti--Explosion-red.svg)]()

> **大佬专属座驾**：拒绝臃肿的传统 Web 面板，在纯血 Bash 终端里加载“四层防弹装甲”。引入智能路由、防爆盘机制、内存级管道及纯 C / AWK 底层高性能计算，打造降维打击级别的 VPS 集群调度中枢。

---

## 🧭 核心哲学：架构师的克制与解耦

VeloX 从不搞“全家桶”式的流氓捆绑，它只做高维的 **[控制平面 (Control Plane)]**。
* **零内存开销**：日常不常驻任何进程，用完即走，对 512M 内存的低配小鸡进行绝对的“内存穿透保护”。
* **物理级生态解耦**：节点核心、Nginx 容器、私有仓环境拥有各自独立的自愈生命周期。VeloX 负责精准提取它们的灵魂，不打包一丝“冗余垃圾脂肪”。

---

## 🗺️ 核心板块矩阵 (Matrix)

### [ 板块一：🛡️ 系统核心运维 ]
基础家底点验快照。集成静态内存精细化报告、磁盘开销雷达、全域监听端口探针。

### [ 板块二：🚀 网络高阶调优 ]
* **出站多维体检**：毫秒级探测 WARP 网卡及 Argo 隐秘隧道的活体连通性，自动捕获动态随机端口。
* **BBR 独立防线**：彻底摒弃污染主 `/etc/sysctl.conf` 的旧时代陋习，采用模块化 `/etc/sysctl.d/99-velox-bbr.conf` 独立文件，卸载即焚，绝不弄脏内核。
* **原子级清理**：强行释放 PageCache/Dentries 内存碎片，Systemd 历史日志物理截断，防爆盘。

### [ 板块三：🔌 代理核心管理 ]
* **深空出站探测**：解锁 Netflix、ChatGPT 全场景流媒体检测，精准评估 IP 纯净度与欺诈风险分数。
* **极限带宽压榨**：针对 Hysteria2 / VLESS-gRPC 进行 TCP/UDP 读写缓冲区与网卡调度队列扩展（智能挂载 CAKE/FQ 算法）。
* **开机自愈秒报**：部署 SSH 异地登录毫秒级 TG 预警，联动 Systemd 挂载开机秒级自愈播报防线。

### [ 板块四：⚙️ 系统防御与自动化运维 ]
* **C语言内核流量管家**：彻底废弃臃肿的 bc 外挂，强行注入 `vnstat` C内核并利用纯 AWK 算法在内存中进行隐式浮点运算。高频分钟级保鲜，支持单向/双向熔断红黄线，超标自动对 TG 司令部发送“绝杀信”。
* **双核防爆武器库**：一行指令部署 [特种兵级 0 内存纯 Bash 机枪塔] 或 [工业正规军 Fail2Ban 装甲]，连续输错 5 次 SSH 密码的黑客 IP 直接物理拉黑。

### [ 板块五：📡 全域高维容灾与资产审计 ]
* **时空调度中心**：全内存管道式覆写 Crontab 自动化齿轮，缝死输出，杜绝本地邮件垃圾炸脑。
* **避让式 Acme 续签**：向签发机构请求续签时，智能注入 Hook 钩子。自动执行 `fuser -k 80/tcp` 端口强行避让，续签成功后全自动秒级热重载代理服务，实现断流秒连。
* **模块化精准克隆 (23号项目)**：
  打破一锅端打包的粗暴逻辑，雷达扫描资产后进行“交互式点餐”。可单独、无损地将你的 **GCP Nginx 私有脚本分发地堡**（包含 `sync_github.sh`、私仓 Token 兵符、`stealth.conf`）以及 Cron 定时任务、TG 报警公共池完美抽离！
  * 强行排除压缩包自身，规避递归死锁警告。
  * 采用 `tar -czpPf` 算子，强行锁死文件的**绝对路径**与**内核级读写权限**。在新机器上只需执行一句长指令，连配置带灵魂，瞬间满血复活！
* **全域资产雷达 (24号项目)**：
  Linux 运维失忆症的终极克星。利用反向系统服务过滤法（黑名单排除），将出厂自带的服务过滤。3秒内对系统进行“照 X 光”式的底层穿透：抓出神秘监听端口、Docker 容器集群以及 FHS 标准高体积藏匿点（Top 6 盘点）。

---

## 🔐 防呆设计与 Input 过滤装甲

本面板内置高强度 **Idiot-Proof（防呆拦截）防护网**，即使闭着眼睛在键盘上乱滚，也绝不会写烂系统底层配置：

1. **正则硬核拦截**：所有数字输入框挂载严格的 `^` 与 `$` 边界锁。敲错一个特殊字符或不小心粘错文本，正则装甲瞬间将其弹开。
2. **逻辑边界死锁**：SSH 端口、Crontab 时间自动进行越界阻断（如端口限制在 1000-65535 且避开保留端口），填错直接打回重填。
3. **基因前缀嗅探**：部署免密登录时，脚本会自动嗅探公钥前缀基因（`ssh-rsa* / ssh-ed25519*`）。哪怕贴错了一句聊天记录，也绝对进不去系统的 `.ssh` 大门。
4. **活体连通性拦截**：绑定 TG 机器人或 Token 时，脚本会实时向 Telegram 司令部发送探测包。只有官方返回 `"ok":true`，凭证才会被写入全局池，拒绝死假凭证。

---

## 📥 极速点火安装

在纯净的 **Ubuntu / Debian / CentOS** 系统中，直接丢入以下指令，一键装载母舰引擎：

```bash
bash <(curl -sL https://raw.githubusercontent.com/pwenxiang51-wq/Velox-VPS-Panel/main/velox.sh)
EOF
```
> **💡 极客提示**：安装完毕后，随时在终端敲入 `velox` 即可唤醒面板！

---

## 🚨 焦土化清理协议 (物理拔管)

当您更换 VPS 或准备迁移阵地时，进入主菜单点击 **`U`** 选项即可触发核平程序。

系统将启动**全功率焦土卸载引擎**：
1. 物理粉碎面板自身脚本、产生的容灾备份包（`Velox_Assets_Backup.tar.gz`），不留一个字节。
2. 彻底拆除 Bash 机枪塔、星际舰队免密兵符、全域 Crontab 定时器。
3. 清洗公共 TG 凭证池，无痕抹除独立网络调优文件，恢复出厂网络内核（Cubic算法）。
4. **严格守护边界**：它只会带走属于 VeloX 产生的痕迹，对您机器上珍贵的常驻应用数据（如您的 Nginx 博客网站、私仓代码）保持绝对的毫发无损！

---

## 👨‍💻 星际指挥官
* **作者 GitHub**: [github.com/pwenxiang51-wq](https://github.com/pwenxiang51-wq)
* **极客博客**: [222382.xyz](https://222382.xyz)
* **Telegram 中枢**: [@Velox95](https://t.me/Velox95)
